apiVersion: v1
kind: Secret
metadata:
  name: app-secret
type: Opaque
stringData:
  # FrontEnd credentials
  FRONTEND_URL: "https://www.windsnow1025.com"

  # PostgreSQL credentials
  POSTGRES_USER: "POSTGRES_USER"
  POSTGRES_PASSWORD: "POSTGRES_PASSWORD"
  POSTGRES_DB: "POSTGRES_DB"
